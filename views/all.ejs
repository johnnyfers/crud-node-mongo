<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    <title>links</title>
</head>

<body>

    <div  class="container-md">
   
    <h1>Links</h1>
    <a href="/add">ADD LINKS</a>

    <% for(let i=0; i < links.length; i++){ %>

        <div class="card p-4 mt-1">
        <div id="<%= links[i]._id %>""   class="d-flex justify-content-between align-items-center" >
            <div class="d-flex flex-column">
                <h2>
                    <%= links[i].title %>
                </h2>
                <div>
                    <%= links[i].description %>
                </div>
                <div>
                  <a class="text-muted" href="<%= links[i].url %>">  <%= links[i].url %> </a> 
                </div>
            </div>
            <div class="d-flex flex-column">
                <button  class="btn btn-danger mb-2" onclick="deleteLink('<%= links[i]._id%>')" )>Delete</button>
                <a class="btn btn-info" href="/edit/<%= links[i]._id %> ">Edit</a>
            </div>
        </div>
        </div>

        <% } %>

        </div>

            <script src="/scripts/script.js"></script>
</body>

</html>